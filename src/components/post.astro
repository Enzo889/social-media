<main>
  <div>
    <img src="src/assets/profile.png" alt="img profile" />
    <span class="container">
      <select id="reply">
        <option value="anyone">Anyone can reply</option>
        <option value="follow">People you follow can reply</option>
        <option value="mention">People you mention can reply</option>
      </select>
      <input type="text" placeholder="What's on your mind..." required />
      <span class="buttons">
        <button class="photo-btn"
          ><svg
            xmlns="http://www.w3.org/2000/svg"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="icon icon-tabler icons-tabler-outline icon-tabler-photo"
            ><path stroke="none" d="M0 0h24v24H0z" fill="none"></path><path
              d="M15 8h.01"></path><path
              d="M3 6a3 3 0 0 1 3 -3h12a3 3 0 0 1 3 3v12a3 3 0 0 1 -3 3h-12a3 3 0 0 1 -3 -3v-12z"
            ></path><path d="M3 16l5 -5c.928 -.893 2.072 -.893 3 0l5 5"
            ></path><path d="M14 14l1 -1c.928 -.893 2.072 -.893 3 0l3 3"
            ></path></svg
          ></button
        >
        <div>
          <button class="cancel-btn">Cancel</button>
          <button class="post-btn">Post</button>
        </div>
      </span>
    </span>
  </div>
</main>

<style>
  main {
    width: 100%;
    border-bottom: 1px solid var(--secondaryBorderColor);

    & div {
      display: flex;
      align-items: center;
      padding: 20px 24px;
      color: var(--grey3);
      column-gap: 12px;
    }
  }

  img {
    width: 48px;
    height: 48px;
    border-radius: 50%;
  }
  .container {
    display: flex;
    flex-direction: column;
    gap: 12px;
    align-items: start;
    justify-content: start;
    width: 100%;
  }

  select {
    border: none;
    font-size: var(--secondary-type-size);
    outline: none;
    color: var(--grey3);
    background: transparent;
    appearance: none;
    cursor: pointer;
    display: none;

    & option {
      font-size: var(--secondary-type-size);
      color: var(--grey1);
      background: var(--inputBackgroundColor);
    }
  }
  input {
    flex: 1;
    border: none;
    font-size: var(--type-size);
    outline: none;
    color: var(--grey3);
    background: transparent;
  }

  .buttons {
    display: flex;
    display: none;
    justify-content: space-between;
    align-items: center;
    width: 100%;

    & div {
      display: flex;
      gap: 8px;
      padding: 0 25px 0 0;
    }

    & button {
      font-size: var(--type-size);
      cursor: pointer;
    }
  }

  .photo-btn {
    color: var(--grey3);
    background: transparent;
    border: none;
    transition: color 0.2s ease-in-out;

    &:hover {
      color: var(--grey2);
    }
  }
  .cancel-btn {
    color: var(--grey1);
    text-underline-offset: 4px;
    text-decoration: none;
    border: none;
    background: transparent;

    &:hover {
      text-decoration: underline;
    }
  }

  .post-btn {
    color: var(--backgroundColor);
    background-color: var(--grey3);
    box-shadow: none;
    pointer-events: none;
    border: none;
    height: 32px;
    line-height: 32px;
    white-space: nowrap;
    padding: 0px 12px;
    border-radius: 8px;
  }

  input:valid + .buttons .post-btn {
    background-color: var(--grey1);
    pointer-events: auto;
    cursor: pointer;
  }

  .container:has(input:valid, input:focus) .buttons,
  .container:has(input:valid, input:focus) select {
    display: flex;
  }
</style>
